# Domain docs

Consumer rules for domain documentation.

## Layout

Single-context: one shared `CONTEXT.md` at the repo root and decisions in `docs/adr/`.

## Consumer rules

- Read `CONTEXT.md` before any task to understand the domain.
- Read `docs/adr/*.md` for architecture decisions.
- Write new ADRs to `docs/adr/` when making significant decisions.
